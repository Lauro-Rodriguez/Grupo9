@model IEnumerable<SIGEFINew.Models.POA.POA_POL_PDOT>

@{
    ViewBag.Title = "Index";
}

<br />
<br />
<h2>Políticas PDOT</h2>

<p>
    Objetivo Estratégico PDOT : @ViewBag.OE_PDOT
</p>

<div class="form-group">
    <div class="row">
        <div class="col-md-1">
            <input type="button" value="Nuevo" onclick="@("location.href='"+ Url.Action("Create","POA_POL_PDOT",new { @id = @ViewBag.OE_PDOT})+ "'")" class="btn btn-primary" />
        </div>
        <div class="col-md-5">
            @using (Html.BeginForm())
            {
                <div class="input-group">
                    @Html.Editor("SearchString", ViewBag.CurrentFilter as string, new { htmlAttributes = new { @class = "form-control", placeholder = "Buscar por Nombre" } })
                    <span class="input-group-btn">
                        <button type="submit" class="btn btn-info">
                            <span class="glyphicon glyphicon-search"></span>
                        </button>
                    </span>

                </div><!-- /input-group -->
            }
        </div>
        <div class="col-md-2">
            <button type="submit" class="btn btn-primary">
                <span class="glyphicon glyphicon-print"></span> Imprimir
            </button>
        </div>
    </div>

</div>
<table class="table table-striped table-bordered table-hover table-condensed">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.COD_POL_PDOT)
        </th>
        <th>
            Objetivo Estratégico
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOM_POL_PDOT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.COD_POL_PDOT)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.POA_OE_PDOT.NOM_OE_PDOT)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.NOM_POL_PDOT)
    </td>
    <td>
        @Html.ActionLink("Editar", "Edit", new { id = item.COD_OE_PDOT, id2 = item.COD_POL_PDOT }) |
        @Html.ActionLink("Borrar", "Delete", new { id = item.COD_OE_PDOT, id2 = item.COD_POL_PDOT }) |
        @Html.ActionLink("Metas", "Details", new { id = item.COD_OE_PDOT, id2 = item.COD_POL_PDOT }) |
    </td>
</tr>
}

</table>
